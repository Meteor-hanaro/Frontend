import React, { useEffect, useRef, useState } from 'react';
import Sign from '../Sign';

const SignPage = () => {
  return (
    <div id="container">
      <Sign />
    </div>
  );
};

export default SignPage;
